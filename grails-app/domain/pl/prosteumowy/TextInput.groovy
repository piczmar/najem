package pl.prosteumowy

class TextInput extends Input {
    String type = "input_text" //TODO: make it enum but fix JSON conversion to display properly in AJAX response

    static constraints = {
    }
}
